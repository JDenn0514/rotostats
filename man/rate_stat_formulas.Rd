% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate-stat-formulas.R
\name{rate_stat_formulas}
\alias{rate_stat_formulas}
\title{Built-in blended-pool rate-stat formula registry}
\usage{
rate_stat_formulas()
}
\value{
A named list. Names are uppercase rate-stat category names (with
\code{K/9}, \code{BB/9}, \code{HR/9} kept in their canonical slashed form); values are
five-field lists as documented in Details.
}
\description{
Returns the named list of blended-pool formula descriptors consumed by
\code{\link[=sgp]{sgp()}} for linear rate stats. Each entry describes how to turn a player's
per-IP (or per-AB) rate into a poolable numerator, how to blend the player
into the team pool, and which direction (higher-is-better vs lower-is-
better) the resulting SGP takes.
}
\details{
Each entry is a list with five fields:

\describe{
\item{\code{denominator_col}}{Character scalar. Name of the playing-time
column in \code{projections} used both as numerator weight and as pool
denominator (e.g., \code{"IP"} for pitcher rate stats, \code{"AB"} for AVG).}
\item{\code{scale}}{Numeric scalar. Multiplicative factor applied when
recomposing the blended rate from the summed numerator and summed
denominator. For ERA and the per-9 stats the scale is \code{9}; for WHIP,
AVG, FIP, xFIP, SIERA, and xERA the scale is \code{1}.}
\item{\code{numerator_fn}}{Function \verb{function(rate, denom) -> numeric}.
Turns a player's rate and playing-time values into the poolable
numerator (e.g., \code{ER = ERA * IP / 9}).}
\item{\code{direction}}{Character scalar. Either \code{"inverse"} (lower is
better — ERA, WHIP, FIP, xFIP, SIERA, xERA, BB/9, HR/9) or
\code{"standard"} (higher is better — AVG, K/9). Controls the subtraction
order in the blended-pool SGP formula.}
\item{\code{pool_type}}{Character scalar. Either \code{"pitcher"} (pool sized from
\code{pool_sizes(config)$pitchers} and selected by descending IP) or
\code{"batter"} (pool sized from \code{pool_sizes(config)$batters} and selected
by descending AB or the entry's \code{denominator_col}).}
}

Pass a list of the same shape as \code{rate_stat_formulas} to \code{\link[=sgp]{sgp()}} to
\strong{fully replace} the default registry (for instance to add a custom
linear rate stat). The replacement semantics are intentionally symmetric
with \code{\link[=sgp_denominators]{sgp_denominators()}}'s \code{inverse_categories} argument: a user override
defines the entire effective set, not an augmentation of the package
default. Pass \code{NULL} (the default) to use the package registry.
}
\examples{
rate_stat_formulas()[["ERA"]]

}
\seealso{
\code{\link[=sgp]{sgp()}}, \code{\link[=rate_stat_denominators]{rate_stat_denominators()}}
}
