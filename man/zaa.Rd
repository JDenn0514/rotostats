% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zaa.R
\name{zaa}
\alias{zaa}
\title{Z-Scores Above Average}
\usage{
zaa(
  stats = NULL,
  config = NULL,
  replacement = NULL,
  pitcher_pool = "combined",
  hitter_pool = "positional",
  category_weight = NULL,
  weight_method = "none",
  ...
)
}
\arguments{
\item{stats}{A data frame of full-season projected or actual statistics.
Must include all \code{config$categories} columns and, when ERA, WHIP, or AVG
are scored, \code{IP} and \code{AB} as full-season totals. Superseded by
\code{attr(replacement, "projections")} when \code{replacement} is supplied.}

\item{config}{A \code{league_config} object from \code{\link[=league_config]{league_config()}}. Superseded by
\code{attr(replacement, "config")} when \code{replacement} is supplied.}

\item{replacement}{Optional. A \code{replacement_level()} output object. When
supplied, restricts the player pool to the rostered pool it defines and
provides \code{stats} and \code{config} via attributes. When \code{NULL}, all rows in
\code{stats} are used and a notice is emitted.}

\item{pitcher_pool}{Character. One of \code{"combined"} (default), \code{"split"}, or
\code{"none"}. Controls whether SP and RP share a z-score pool.
\code{"combined"}: all pitchers in one pool (LPP/FanGraphs approach).
\code{"split"}: SP and RP form separate pools (FVARz/BIGz approach).
\code{"none"}: all pitchers in one pool, no SP/RP distinction.}

\item{hitter_pool}{Character. One of \code{"positional"} (default) or
\code{"combined"}. Controls whether hitter z-scores are computed within each
position (\code{"positional"}) or across all hitters together (\code{"combined"}).}

\item{category_weight}{Named numeric vector mapping position pool labels
(e.g., \code{c(SP = 0.8, RP = 0.8)}) to manual multipliers applied to
\code{total_zaa}. Overrides \code{weight_method} when supplied.}

\item{weight_method}{Character. One of \code{"none"} (default), \code{"linear"}, or
\code{"sqrt"}. Auto-computes category-count normalization for \code{total_zaa}.
Ignored when \code{category_weight} is supplied.}

\item{...}{Reserved for future use.}
}
\value{
A data frame with one row per player in the selected pool, columns
\verb{zaa_<CAT>} for each scored category in \code{config$categories} order, and
\code{total_zaa}. If \code{player_id} is present in the working stats, it is
prepended as the first column. Three attributes are set:
\describe{
\item{\code{attr(result, "units")}}{\code{"zscore"}}
\item{\code{attr(result, "anchor")}}{\code{"average"}}
\item{\code{attr(result, "distribution")}}{Nested or flat list of per-pool,
per-category distribution parameters. For counting stats: \code{list(mean, sd)}. For rate stats: \code{list(mean, sd, sd_vol)} where \code{sd_vol} is the
population SD of the volume-weighted z-scores (Step 2b denominator),
used by \code{zar()} to score replacement lines without re-running \code{zaa()}.
Schema: nested (position-keyed -> category-keyed) when
\code{hitter_pool = "positional"}; flat (category-keyed) when
\code{hitter_pool = "combined"}. Pitchers: nested when
\code{pitcher_pool = "split"}, flat otherwise.}
}
}
\description{
Computes per-player, per-category z-scores above the within-position average
for rotisserie baseball projection data. The anchor is the position mean:
a player at the position mean gets \code{total_zaa = 0}.

Rate stats (ERA, WHIP, and all \code{INVERSE_CATEGORIES}, plus AVG) receive a
two-step volume-weighted treatment: a raw z-score is computed from the
unweighted mean and SD, then multiplied by the player's volume statistic
(IP for ERA/WHIP-family; AB for AVG) and re-standardized using the
population SD of those volume-weighted values within the pool. ERA and
WHIP z-scores are negated so that positive z = positive standings
contribution.

Counting stats (HR, R, RBI, SB, K, W, SV, HLD, QS, SVHD, etc.) use the
unweighted population mean and SD directly.
}
\section{Sign convention}{

ERA, WHIP, and all \code{\link[=inverse_categories]{inverse_categories()}} are negated: a positive z-score
means a positive contribution to standings. AVG and all counting stats are
NOT negated. Column names do not encode direction; consumers must respect this
convention.
}

\section{Pool definition}{

When \code{replacement} is supplied, only players in
\code{attr(replacement, "position_assignments")} are scored; fringe players below
the replacement boundary are excluded. When \code{replacement = NULL}, all rows in
\code{stats} are used and within-position SD may be inflated by marginal players.
}

\examples{
# Minimal example with explicit stats and config
config <- league_config(
  n_teams = 12L,
  roster_slots = c(
    "C" = 1,
    "1B" = 1,
    "2B" = 1,
    "3B" = 1,
    "SS" = 1,
    "OF" = 3,
    "DH" = 1
  ),
  pitcher_slots = 9,
  budget = 260L,
  budget_split = 0.67,
  categories = c("HR", "R")
)
# proj <- <data frame of projections>
# result <- zaa(stats = proj, config = cfg)

}
\seealso{
\code{\link[=par]{par()}}, \code{\link[=zar]{zar()}}, \code{\link[=sgp]{sgp()}}, \code{\link[=replacement_level]{replacement_level()}}, \code{\link[=league_config]{league_config()}},
\code{\link[=inverse_categories]{inverse_categories()}}
}
