% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replacement.R
\name{replacement_level}
\alias{replacement_level}
\title{Per-position replacement-level stat lines}
\usage{
replacement_level(
  projections,
  config,
  sort_by = "zscore",
  sgp_denominators = NULL,
  boundary_method = "head_count",
  seed_method = "hierarchy",
  positional_adjustment_method = "fvarz",
  pos_weight = NULL,
  boundary_rate_method = "raw_ip",
  cliff_method = "mad",
  cliff_gap_ratio_threshold = 0.375,
  catcher_adjustment_method = "split_pool",
  sp_ip_threshold = 100,
  normalize_to_season = FALSE,
  band_width = NULL,
  cliff_threshold = 1.5,
  rate_denominators = NULL,
  league_history = NULL,
  trim_method = "iqr",
  multi_pos = "highest_par",
  position_assignments = NULL,
  replacement_params = list(),
  max_iter = 25L,
  tol = 0.01,
  verbose = FALSE
)
}
\arguments{
\item{projections}{A data frame with one row per projected player.
Required columns: \code{player_id}, \code{player_name}, \code{pos_eligibility},
\code{team}, \code{league}, plus one column per scored category in \code{config$categories}.
Pitcher rows require \code{IP}; hitter rows require \code{AB} when \code{AVG}, \code{OBP}, or
\code{SLG} is a scored category.  Column names are normalized to uppercase at
entry (no message emitted).  \code{pos_eligibility} is pipe-delimited
(\code{"C"}, \code{"1B|3B"}, \code{"SP|RP"}); the first entry is the primary position.
Optional \code{role} column (\code{"SP"} / \code{"RP"}) overrides IP-based inference.}

\item{config}{A \code{league_config} object from \code{league_config()}.}

\item{sort_by}{Character scalar.  \code{"zscore"} (default) — composite z-score
across projected stats; or \code{"sgp"} — total SGP from \code{sgp()}.  When
\code{sort_by = "sgp"}, \code{sgp_denominators} is required.}

\item{sgp_denominators}{An \code{sgp_denominators} S3 object from
\code{sgp_denominators()}.  Required when \code{sort_by = "sgp"}.  Also used
when \code{boundary_rate_method = "sgp_pool"}.}

\item{boundary_method}{Character scalar.  \code{"head_count"} (default) —
boundary at \verb{n_teams × roster_slots[pos]}; or \code{"playing_time"} —
boundary at the player whose cumulative PA/IP crosses the league
deployment threshold.}

\item{seed_method}{Character scalar.  \code{"hierarchy"} (default) — pass-1
seed uses scarcity ordering C > SS > 2B > 3B > 1B > OF; or
\code{"historical_priors"} — seeds from \code{league_history} replacement-level
z-scores (requires \code{league_history}).}

\item{positional_adjustment_method}{Character scalar.
\code{"fvarz"} (default), \code{"sgp"}, \code{"dollar"}, or \code{"posblend"}.  Controls
how scarcity premiums are computed.}

\item{pos_weight}{Numeric in \verb{[0, 1]} or \code{NULL}.  Required when
\code{positional_adjustment_method = "posblend"}.}

\item{boundary_rate_method}{Character scalar.  \code{"raw_ip"} (default) —
IP-weighted rate-stat contributions independent of league depth; or
\code{"sgp_pool"} — pool-marginal method (requires \code{sgp_denominators}
calibrated with \code{rate_conversion = "blended_pool"}).}

\item{cliff_method}{Character scalar.  \code{"mad"} (default), \code{"fisher_jenks"},
or \code{"gap_ratio"}.  Controls cliff detection algorithm.}

\item{cliff_gap_ratio_threshold}{Numeric in \verb{(0, 1)}.  Threshold for
\code{cliff_method = "gap_ratio"} (default \code{0.375}).}

\item{catcher_adjustment_method}{Character scalar.  \code{"split_pool"}
(default), \code{"positional_default"}, \code{"partial_offset"}, or \code{"none"}.
Controls catcher-specific scarcity-premium override.}

\item{sp_ip_threshold}{Numeric.  IP cutoff for SP vs. RP inference when
no \code{role} column is present (default \code{100}).}

\item{normalize_to_season}{Logical.  If \code{TRUE}, normalize counting stats
to full-season baselines (hitters 600 PA / 550 AB, SP 200 IP, RP 70 IP).
Default \code{FALSE}.}

\item{band_width}{Integer or \code{NULL}.  Band half-width K.  Overrides
\code{replacement_params$band_width_K} when non-NULL.}

\item{cliff_threshold}{Numeric.  Cliff detection threshold (default \code{1.5}).
Overrides \code{replacement_params$cliff_threshold}.}

\item{rate_denominators}{Named character vector or \code{NULL}.  Extends or
overrides the built-in rate-stat denominator lookup (see
\code{\link[=rate_stat_denominators]{rate_stat_denominators()}}).}

\item{league_history}{A \code{league_history} object from \code{league_history()}
or \code{NULL}.  Used for \$1 calibration cross-check and IP/AB divergence
validation.  Required when \code{seed_method = "historical_priors"}.}

\item{trim_method}{Character scalar.  \code{"iqr"} (default), \code{"mad"}, or
\code{"kde"}.  Controls outlier trimming of the \$1 auction pool from
\code{league_history$prices} when \code{is_keeper} column is absent.}

\item{multi_pos}{Character scalar.  \code{"highest_par"} (default), \code{"primary"},
\code{"all"}, or \code{"custom"}.  Controls multi-eligible position assignment.}

\item{position_assignments}{Named character vector (\code{player_id} → position)
or \code{NULL}.  Supplied by \code{dollar_values()} on pass 2+; \code{NULL} on first
call.}

\item{replacement_params}{Named list.  Override specific entries of
\code{default_replacement_params} (e.g., \code{list(band_width_K = 2L)}).
\code{cycle_history_window} (integer, default 5L): depth of the rolling
assignment-hash history window used to detect higher-order convergence
cycles (periods 2 through N) in the \code{multi_pos = "highest_par"} loop.
Increase if you observe non-convergence in pools with many near-boundary
multi-eligible players.}

\item{max_iter}{Positive integer.  Maximum convergence passes (default \code{25L}).}

\item{tol}{Numeric.  Convergence tolerance in SGP units (default \code{0.01}).}

\item{verbose}{Logical.  If \code{TRUE}, emit diagnostic messages and
verbose-gated warnings.  Default \code{FALSE}.}
}
\value{
A named list with elements:
\describe{
\item{\code{replacement_stats}}{data.frame — one row per position (positions
with \code{roster_slots > 0}, plus SP and RP).  Columns: \code{position},
one column per scored category, \code{IP} for pitcher rows, \code{AB} for hitter
rows when AVG/OBP/SLG scored, \code{n_band_players}, \code{cliff_detected}.}
\item{\code{positional_adjustments}}{Named numeric vector of scarcity
premiums by position, or \code{NULL} on pass 1 when
\code{positional_adjustment_method = "sgp"}.}
\item{\code{cliff_metric}}{data.frame — one row per position with columns
\code{position}, \code{cliff_detected}, \code{cliff_location}, \code{cliff_magnitude},
\code{swingman}, \code{n_band_players}.}
\item{\code{two_way_players}}{Character vector of \code{player_id}s with PAR > 0
in both hitter and pitcher roles.}
\item{\code{pool_diagnostics}}{List with \code{position_sd_ratio}: named numeric
vector of within-position SD / global SD per scored category.}
\item{\code{method}}{\code{"boundary_band"}.}
\item{\code{params}}{Named list — convergence info, band/cliff settings,
iteration count, stat_units.}
}
Attributes: \code{stat_units}, \code{config}, \code{projections}, \code{position_assignments},
\code{converged}, \code{iterations}, \code{delta}.
}
\description{
Computes per-position replacement-level stat lines that serve as the
zero-dollar baseline for PAR (Points Above Replacement) — the direct
analog of WAR in rotisserie auction valuation.  A player who projects to
exactly replacement level at their position has \$0 value; positive PAR
drives auction dollar values.

The roster boundary at each position is the player at rank
\verb{n_teams × roster_slots[pos]} in the sorted projection pool.  A symmetric
boundary band of ±K players around the boundary smooths cliff effects from
individual projection outliers.  Cliff detection truncates the band at
genuine talent gaps.  An internal iteration loop (controlled by
\code{max_iter} and \code{tol}) resolves the circularity between player ranking and
replacement level when \code{sort_by = "sgp"} or \code{multi_pos = "highest_par"}.
}
\examples{
\dontrun{
cfg <- league_config(
  n_teams       = 2L,
  roster_slots  = c(C = 1L, `1B` = 1L),
  pitcher_slots = c(SP = 1L, RP = 1L),
  batting_categories = c("HR", "RBI"),
  pitcher_categories = character(0L),
  budget        = 260L
)

proj <- data.frame(
  player_id       = paste0("p", 1:8),
  player_name     = paste0("Player", 1:8),
  pos_eligibility = c("C", "C", "C", "1B", "1B", "1B", "SP", "RP"),
  team            = rep("TM", 8),
  league          = rep("AL", 8),
  HR              = c(15, 12, 9, 30, 28, 25, 10, 3),
  RBI             = c(50, 45, 40, 90, 88, 85, 35, 15),
  IP              = c(NA, NA, NA, NA, NA, NA, 180, 65),
  AB              = c(350, 320, 300, 500, 490, 480, NA, NA),
  stringsAsFactors = FALSE
)

result <- replacement_level(proj, cfg)
result$replacement_stats
}

}
\seealso{
\code{replacement_from_prices()}, \code{default_replacement_params},
\code{rate_stat_denominators()}, \code{league_config()}, \code{sgp_denominators()}
}
