% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/league-history.R
\name{league_history}
\alias{league_history}
\title{Historical league data for rotisserie valuation}
\usage{
league_history(team_season, prices = NULL)
}
\arguments{
\item{team_season}{A data frame in wide format, one row per team-year, with
columns \code{year}, \code{team_id}, and one column per scored category. Stat
column names are normalized to uppercase at construction.}

\item{prices}{Optional data frame of historical auction results with
columns \code{year}, \code{player_name}, \code{price}, and optional \code{player_id},
\code{is_keeper}, \code{player_type}.}
}
\value{
An S3 object of class \code{c("league_history", "list")} with
components \verb{$team_season} and \verb{$prices} (NULL if not supplied).
}
\description{
Builds a validated S3 list that holds team-season category totals and,
optionally, historical auction prices. Passed as \code{league_history} to
\code{\link[=sgp_denominators]{sgp_denominators()}} and \code{replacement_level()}. Construct once and reuse.

Holds only historical data. Window and exclusion choices remain as
arguments on the consumer (\code{exclude_years}, \code{outlier_filter}, \code{years}).
The exception is \code{prices}: no window parameter is provided, so the caller
must pre-filter prices to the desired years (e.g., exclude 2020).
}
\examples{
ts <- data.frame(
  year    = rep(2022:2023, each = 2),
  team_id = rep(c("A", "B"), 2),
  HR      = c(180, 210, 190, 220)
)
h <- league_history(team_season = ts)
print(h)
}
\seealso{
\code{\link[=league_config]{league_config()}}, \code{\link[=sgp_denominators]{sgp_denominators()}}
}
