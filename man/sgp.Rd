% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sgp.R
\name{sgp}
\alias{sgp}
\title{Convert player projections to SGP units}
\usage{
sgp(
  projections,
  denominators,
  league_history = NULL,
  rate_conversion = "blended_pool",
  pool_baseline = "projection_pool",
  league_config = NULL,
  baseline_era = NULL,
  baseline_whip = NULL,
  baseline_avg = NULL
)
}
\arguments{
\item{projections}{A data frame with one row per player. Must include a
column for each scored category in \code{names(denominators)}, plus \code{IP} (for
ERA/WHIP pool construction) and \code{AB} (for AVG pool construction) when
those rate stats are scored. Column names are normalized to uppercase at
entry.}

\item{denominators}{An \code{sgp_denominators} S3 object from
\code{\link[=sgp_denominators]{sgp_denominators()}}. The scored categories are \code{names(denominators)}.
The attribute \code{attr(denominators, "rate_conversion")} is read from the
outer S3 object (not from \code{denominators$denominators}).}

\item{league_history}{A \code{league_history} S3 object or duck-typed list with
a \verb{$team_season} data frame. Required when
\code{rate_conversion = "blended_pool"}. Must contain \code{IP} and \code{AB} columns
alongside rate-stat columns in \verb{$team_season}.}

\item{rate_conversion}{Character. One of \code{"blended_pool"} (default),
\code{"fixed_baseline"}, \code{"per_player"}, \code{"universal_constants"}, or
\code{"team_ip_normalized"}. The last three abort with
\code{rotostats_error_not_implemented}. \code{"fixed_baseline"} delegates to
\code{\link[=convert_rate_stats]{convert_rate_stats()}}. Must match \code{attr(denominators, "rate_conversion")}
when \code{"blended_pool"}.}

\item{pool_baseline}{Character. Currently only \code{"projection_pool"} is
implemented.}

\item{league_config}{A \code{league_config} S3 object from \code{\link[=league_config]{league_config()}}.
Required when \code{rate_conversion = "blended_pool"} and
\code{pool_baseline = "projection_pool"}. Passed to \code{\link[=pool_sizes]{pool_sizes()}} to derive
\code{pool_size_p} and \code{pool_size_h}.}

\item{baseline_era}{Numeric scalar. Explicit ERA baseline for
\code{rate_conversion = "fixed_baseline"}. Ignored for \code{"blended_pool"}.}

\item{baseline_whip}{Numeric scalar. Explicit WHIP baseline. Ignored for
\code{"blended_pool"}.}

\item{baseline_avg}{Numeric scalar. Explicit AVG baseline. Ignored for
\code{"blended_pool"}.}
}
\value{
A data frame with one row per player (same order as \code{projections}),
one column \verb{sgp_<CAT>} per scored category (e.g., \code{sgp_HR}, \code{sgp_ERA}),
and a final column \code{total_sgp} equal to \code{rowSums()} across all
\verb{sgp_<CAT>} columns. \code{total_sgp} is \code{NA} for any player whose
per-category SGP is \code{NA} (propagation via \code{na.rm = FALSE}).
}
\description{
Converts projected per-player statistics into SGP (Standings Gain Points)
units using pre-calibrated denominators from \code{\link[=sgp_denominators]{sgp_denominators()}}. The
primary method is \code{"blended_pool"} with \code{pool_baseline = "projection_pool"},
which derives blended-pool rate-stat baselines from the top-N projected
starters by playing time.
}
\seealso{
\code{\link[=sgp_denominators]{sgp_denominators()}}, \code{\link[=league_config]{league_config()}}, \code{\link[=league_history]{league_history()}}
}
