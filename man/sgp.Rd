% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sgp.R
\name{sgp}
\alias{sgp}
\title{Convert projected stats into SGP units per player}
\usage{
sgp(
  projections,
  denominators,
  league_history = NULL,
  rate_conversion = "blended_pool",
  pool_baseline = "projection_pool",
  league_config = NULL,
  baseline_era = NULL,
  baseline_whip = NULL,
  baseline_avg = NULL
)
}
\arguments{
\item{projections}{A data frame with one row per player. Must include a
column for each scored category in \code{names(denominators)}, plus \code{IP} (for
ERA/WHIP pool construction) and \code{AB} (for AVG pool construction) when
those rate stats are scored. Column names are normalized to uppercase at
entry; no message is emitted for the normalization.}

\item{denominators}{An \code{sgp_denominators} S3 object produced by
\code{\link[=sgp_denominators]{sgp_denominators()}}. The scored categories are \code{names(denominators)}.
\strong{Important:} \code{attr(denominators, "rate_conversion")} is read from the
outer S3 object — not from \code{denominators$denominators} — and must match
the \code{rate_conversion} argument when \code{rate_conversion = "blended_pool"}.}

\item{league_history}{A \code{league_history} S3 object (see \code{\link[=league_history]{league_history()}})
or a duck-typed list with a \verb{$team_season} data frame.  Required when
\code{rate_conversion = "blended_pool"}.  \verb{$team_season} must contain \code{IP} and
\code{AB} columns alongside the rate-stat columns so that IP- and AB-weighted
baseline means can be computed.}

\item{rate_conversion}{Character scalar.  One of \code{"blended_pool"} (default),
\code{"fixed_baseline"}, \code{"per_player"}, \code{"universal_constants"}, or
\code{"team_ip_normalized"}.  The last three abort with
\code{rotostats_error_not_implemented}.  \code{"fixed_baseline"} delegates to
\code{\link[=convert_rate_stats]{convert_rate_stats()}} (stub; also aborts).  When \code{"blended_pool"},
\code{attr(denominators, "rate_conversion")} must equal \code{"blended_pool"} or
\code{sgp()} aborts with \code{rotostats_error_invalid_rate_conversion}.}

\item{pool_baseline}{Character scalar.  Determines how pool constants are
constructed for the blended-pool rate-stat formulas.  Currently only
\code{"projection_pool"} (the default) is implemented.}

\item{league_config}{A \code{league_config} S3 object from \code{\link[=league_config]{league_config()}}.
Required when \code{rate_conversion = "blended_pool"} and
\code{pool_baseline = "projection_pool"}.  Passed to \code{pool_sizes()} (an
internal helper in \code{R/league-config.R}) to derive \code{pool_size_p} and
\code{pool_size_h} from the league's roster structure rather than hard-coding
roster depth.}

\item{baseline_era}{Numeric scalar or \code{NULL}.  Explicit ERA baseline used
only when \code{rate_conversion = "fixed_baseline"}.  Passed through to
\code{\link[=convert_rate_stats]{convert_rate_stats()}}; ignored for \code{"blended_pool"}.}

\item{baseline_whip}{Numeric scalar or \code{NULL}.  Explicit WHIP baseline.
See \code{baseline_era}.}

\item{baseline_avg}{Numeric scalar or \code{NULL}.  Explicit AVG baseline.
See \code{baseline_era}.}
}
\value{
A plain \code{data.frame} (no attributes) with:
\describe{
\item{\verb{sgp_<CAT>}}{One numeric column per scored category, named
\verb{sgp_<CAT>} where \verb{<CAT>} is the uppercase category name
(e.g., \code{sgp_HR}, \code{sgp_ERA}, \code{sgp_AVG}).  Column order matches
\code{names(denominators)}.  Players missing a scored category column, or
with 0 / NA projected playing time for a rate stat, receive \code{NA} in
that column.}
\item{\code{total_sgp}}{\code{rowSums()} across all \verb{sgp_<CAT>} columns
(\code{na.rm = FALSE}).  \code{NA} when any per-category SGP is \code{NA}.}
}
Row order matches \code{projections}.
}
\description{
Converts per-player projected statistics into SGP (Standings Gain Points)
units using pre-calibrated denominators from \code{\link[=sgp_denominators]{sgp_denominators()}}.  Each
category SGP measures how many standings places that player's projected
contribution is worth in your league.
}
\details{
\subsection{Counting categories}{

For every non-rate scored category \code{c}, the conversion is a simple vectorized
division applied across all players at once:

\deqn{SGP[i, c] = projected[i, c] \;/\; denominator[c]}

No player-level loop is used; computation is fully vectorized.
}

\subsection{Rate categories (ERA, WHIP, AVG) — blended-pool method}{

With \code{rate_conversion = "blended_pool"} and
\code{pool_baseline = "projection_pool"} (the defaults), rate-stat SGP is
computed via the \emph{blended-pool} marginal contribution formula.  Pool
constants (\code{pool_ER}, \code{pool_IP}, \code{pool_WH}, \code{pool_H}, \code{pool_AB}) are
derived once from the top-\code{pool_size_p} / top-\code{pool_size_h} projected
players by playing time (IP for pitchers, AB for hitters).  Pool sizes
are taken from \code{pool_sizes(league_config)} and therefore automatically
reflect your league's roster structure.

The per-player formulas are:

\deqn{ERA\_SGP[i]  = (avg\_ERA  - (pool\_ER  + player\_ER)  \times 9 \;/\; (pool\_IP + player\_IP)) \;/\; d_{ERA}}
\deqn{WHIP\_SGP[i] = (avg\_WHIP - (pool\_WH  + player\_WH)  \;/\; (pool\_IP + player\_IP)) \;/\; d_{WHIP}}
\deqn{AVG\_SGP[i]  = ((pool\_H  + player\_H)  \;/\; (pool\_AB + player\_AB) - avg\_AVG) \;/\; d_{AVG}}

Note the sign flip for AVG: higher batting average helps the team, whereas
higher ERA or WHIP hurts it, so the subtraction order is reversed.

\code{avg_ERA}, \code{avg_WHIP}, and \code{avg_AVG} are derived from the most recent
non-excluded year in \code{league_history$team_season} using IP-weighted (ERA,
WHIP) or AB-weighted (AVG) means across all teams.  A \code{cli_inform()} message
names the year used so callers can verify the baseline.
}

\subsection{\code{total_sgp}}{

\code{total_sgp} is \code{rowSums()} across all \verb{sgp_<CAT>} columns with
\code{na.rm = FALSE}: any player with a missing per-category SGP also has a
missing \code{total_sgp}.  This surfaces data quality issues rather than hiding
them in a partial sum.
}

\subsection{Validation order}{

Checks are applied in this order so that the most specific error wins:
\enumerate{
\item \code{rate_conversion} must be one of the five recognized values (else
\code{rotostats_error_invalid_rate_conversion}).
\item When \code{rate_conversion = "blended_pool"}, \code{attr(denominators, "rate_conversion")} must also be \code{"blended_pool"} — mismatched units
abort with \code{rotostats_error_invalid_rate_conversion}.
\item \code{"per_player"}, \code{"universal_constants"}, \code{"team_ip_normalized"} abort
with \code{rotostats_error_not_implemented}.
\item \code{"fixed_baseline"} delegates to \code{\link[=convert_rate_stats]{convert_rate_stats()}} (stub; also aborts
with \code{rotostats_error_not_implemented}).
\item Required inputs (\code{league_history}, \code{league_config}) are validated only
after the method is confirmed.
}
}

\subsection{SVHD handling}{

If \code{"SVHD"} is a scored category but the column is absent from \code{projections},
\code{sgp()} attempts to derive it as \code{SV + HLD} (also accepts \code{HD} as the holds
column name).  A one-time informational message is emitted via
\code{rlang::inform()} asking the caller to verify the definition matches their
league's hold rules.
}

\subsection{Upstream helper}{

\code{sgp()} consumes the output of \code{\link[=sgp_denominators]{sgp_denominators()}}.  See that function for
how denominators are calibrated from historical team-season standings data.
}
}
\section{Warnings}{


\code{sgp()} emits \code{rotostats_warning_missing_category_column} (via
\code{\link[cli:cli_abort]{cli::cli_warn()}}) in two situations:
\enumerate{
\item A scored category column is entirely absent from \code{projections} —
the affected \verb{sgp_<CAT>} column is filled with \code{NA}.
\item A player has 0 or \code{NA} projected IP (when ERA or WHIP is scored) or
projected AB (when AVG is scored) — that player's rate-stat SGP is
set to \code{NA}.
}
}

\examples{
# Toy example: two counting categories, three players
history <- list(
  team_season = data.frame(
    year    = c(2022L, 2022L, 2022L, 2023L, 2023L, 2023L),
    team_id = rep(c("A", "B", "C"), 2),
    HR      = c(150, 180, 210, 155, 185, 215),
    R       = c(650, 700, 740, 660, 710, 750),
    IP      = c(1350, 1380, 1410, 1360, 1390, 1420),
    AB      = c(5400, 5500, 5600, 5420, 5520, 5620),
    stringsAsFactors = FALSE
  )
)

config <- league_config(
  n_teams       = 12L,
  roster_slots  = c(C = 1L, `1B` = 1L, `2B` = 1L, `3B` = 1L,
                    SS = 1L, OF = 3L, DH = 1L),
  pitcher_slots = 9L,
  budget        = 260L,
  budget_split  = 0.67,
  categories    = c("HR", "R")
)

denoms <- sgp_denominators(
  history,
  scoring_categories = c("HR", "R"),
  exclude_years      = integer(0)
)

projections <- data.frame(
  HR = c(40L, 25L, 10L),
  R  = c(90L, 80L, 70L),
  IP = c(0L,  0L,  0L),    # pitching not scored — zeroes are fine
  AB = c(500L, 450L, 400L)
)

\dontrun{
# blended_pool requires league_history and league_config:
result <- sgp(
  projections   = projections,
  denominators  = denoms,
  league_history = history,
  league_config  = config
)
result  # data frame: sgp_HR, sgp_R, total_sgp
}

}
\seealso{
\code{sgp_denominators()} for calibrating the denominators consumed by
this function; \code{\link[=league_config]{league_config()}} for constructing the league configuration
object; \code{\link[=league_history]{league_history()}} for the historical data object.
}
