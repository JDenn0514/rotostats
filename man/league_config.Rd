% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/league-config.R
\name{league_config}
\alias{league_config}
\title{League configuration for rotisserie valuation}
\usage{
league_config(
  n_teams = 12L,
  roster_slots,
  pitcher_slots = 9L,
  batting_categories,
  pitcher_categories,
  inverse_categories = NULL,
  league_type = "mixed",
  budget = 260L,
  budget_split = 0.6,
  keeper = FALSE
)
}
\arguments{
\item{n_teams}{Positive integer. Number of teams in the league. Default 12.}

\item{roster_slots}{Named integer vector of hitter roster slots by position.
Combo slots (UTIL, MI, CI) should be included — they count toward league
depth for pool sizing even though they have no independent replacement
pool. Positions with zero slots may be omitted.}

\item{pitcher_slots}{Either a single integer (total pitcher slots per team;
SP/RP split inferred downstream) or a named integer vector with names
from \code{c("SP", "RP")}. Default \code{9L}.}

\item{batting_categories}{Character vector of scored batting categories.
Normalized to uppercase. Required. Validates against
\code{CANONICAL_BATTING_CATEGORIES}; categories belonging to the canonical
pitcher list emit \code{rotostats_warning_category_side_mismatch}.}

\item{pitcher_categories}{Character vector of scored pitcher categories.
Normalized to uppercase. Required. Validates against
\code{CANONICAL_PITCHER_CATEGORIES}; categories belonging to the canonical
batting list emit \code{rotostats_warning_category_side_mismatch}.}

\item{inverse_categories}{Optional character vector of scoring category
names where a lower value is better (lower-is-better categories, e.g.,
ERA, WHIP, FIP). Normalized to uppercase at construction. Every element
must appear in \code{categories}; unknown names abort with
\code{rotostats_error_invalid_inverse_categories}. \code{NULL} (default) means no
league-level override; \code{\link[=sgp_denominators]{sgp_denominators()}} and future consumers fall
through to the package lookup (\code{\link[=inverse_categories]{inverse_categories()}}). Pass
\code{character(0)} is not valid; use \code{NULL} for "no override".}

\item{league_type}{One of \code{"mixed"}, \code{"AL"}, \code{"NL"}. Default \code{"mixed"}.
Controls DH eligibility and downstream player-pool filtering: \code{"AL"} /
\code{"NL"} cause \code{\link[=replacement_level]{replacement_level()}} to drop rows where
\code{projections$LEAGUE} does not match before computing the replacement
pool. \code{"NL"} additionally drops \code{DH} from \code{roster_slots} with a warning
if present. \code{"mixed"} keeps both leagues.}

\item{budget}{Positive integer. Per-team auction budget in dollars.
Default \code{260L}.}

\item{budget_split}{Numeric in \verb{(0, 1)}. Fraction of the total league
budget devoted to hitters. Default \code{0.60}. Calibrate from spending
history with \code{calibrate_budget_split()}.}

\item{keeper}{Controls keeper handling. \code{FALSE} (default) = redraft;
\code{TRUE} = keeper league with default settings
(\code{method = "pool_shrink"}, \code{keeper_col = "is_keeper"},
\code{salary_col = NULL}); or an explicit named list with those fields plus
\code{is_keeper = TRUE}.}
}
\value{
An S3 object of class \code{c("league_config", "list")} with the
resolved and validated settings.
}
\description{
Builds a validated S3 list that captures the structural settings of a
rotisserie league: team count, roster shape, scoring format, budget, and
keeper status. Passed as \code{config} to all downstream valuation functions
(\code{\link[=sgp_denominators]{sgp_denominators()}}, \code{sgp()}, \code{replacement_level()}, \code{dollar_values()},
\code{par()}). Construct once per league and reuse.

Structural only — algorithmic choices (denominator method, rate conversion,
weights) remain as arguments on the functions that consume them.
}
\examples{
lg <- league_config(
  n_teams            = 12L,
  roster_slots       = c(C = 1, "1B" = 1, "2B" = 1, "3B" = 1,
                         SS = 1, OF = 5, UTIL = 1),
  pitcher_slots      = c(SP = 6L, RP = 3L),
  batting_categories = c("R", "HR", "RBI", "SB", "AVG"),
  pitcher_categories = c("W", "K", "SV", "ERA", "WHIP")
)
print(lg)
}
\seealso{
\code{\link[=league_history]{league_history()}}, \code{\link[=sgp_denominators]{sgp_denominators()}}
}
