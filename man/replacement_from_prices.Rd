% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replacement.R
\name{replacement_from_prices}
\alias{replacement_from_prices}
\title{Replacement-level stat lines derived from historical \$1 auction prices}
\usage{
replacement_from_prices(
  prices,
  n_teams,
  roster_slots,
  categories,
  trim_method = "iqr",
  calibration_min_n = 15L,
  verbose = FALSE
)
}
\arguments{
\item{prices}{A data frame of historical auction results.  Required columns:
\code{year}, \code{player_name}, \code{price}, \code{pos_eligibility} (pipe-delimited,
e.g., \code{"1B|3B"}).  Optional: \code{player_id}, \code{is_keeper} (logical).
When \code{player_id} is absent, matching falls back to Unicode-normalized
name matching.}

\item{n_teams}{Positive integer.  Number of teams in the league.}

\item{roster_slots}{Named integer vector.  Roster slots per position.}

\item{categories}{Character vector of scored categories.}

\item{trim_method}{Character scalar.  \code{"iqr"} (default) — remove above
Q3 + 1.5×IQR; \code{"mad"} — remove above median + 3×MAD; \code{"kde"} — detect
trough via kernel density estimation (errors if no trough found).
Ignored when \code{is_keeper} is present in \code{prices}.}

\item{calibration_min_n}{Positive integer.  Minimum number of \$1 players
remaining after trimming.  If fewer remain, emits
\code{rotostats_warning_calibration_suppressed} and returns \code{NULL}.
Default \code{15L}.}

\item{verbose}{Logical.  If \code{TRUE}, emit name-match failure warnings.
Default \code{FALSE}.}
}
\value{
Same named list schema as \code{replacement_level()} with
\code{method = "prices"} in \code{params}, or \code{NULL} when calibration is
suppressed.  \code{attr(result, "projections")} is \code{NULL}.
}
\description{
Computes replacement-level stat lines from historical \$1 auction data
rather than from projections.  Returns the same output schema as
\code{replacement_level()} and is interchangeable as the \code{replacement_fn}
argument to \code{dollar_values()}.

The algorithm: filter prices to \code{price <= 1}, exclude known keepers
(via \code{is_keeper} flag when present, else statistical trimming), and
compute the mean end-of-season stat line for each position.  No projection
pool, no sorting, no band, no cliff detection, no SP/RP inference.

Most useful as a validation layer on \code{replacement_level()} output or as
the primary method in leagues with 5+ seasons of low-keeper-density
auction history.
}
\examples{
prices_df <- data.frame(
  year            = c(2023L, 2023L, 2023L, 2023L),
  player_name     = c("Smith J", "Jones M", "Brown K", "White R"),
  price           = c(1L, 1L, 1L, 1L),
  pos_eligibility = c("1B", "SS", "OF", "C"),
  HR              = c(5, 4, 6, 3),
  RBI             = c(22, 18, 24, 15),
  stringsAsFactors = FALSE
)

\dontrun{
result <- replacement_from_prices(
  prices        = prices_df,
  n_teams       = 12L,
  roster_slots  = c(C = 1L, `1B` = 1L, SS = 1L, OF = 3L),
  categories    = c("HR", "RBI")
)
result$replacement_stats
}

}
\seealso{
\code{replacement_level()}
}
