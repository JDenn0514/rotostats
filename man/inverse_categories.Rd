% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inverse-categories.R
\name{inverse_categories}
\alias{inverse_categories}
\title{Built-in inverse-category lookup}
\usage{
inverse_categories()
}
\value{
A character vector of category names (all uppercase). The exact
contents are the package's current starter list; use \code{\link[=league_config]{league_config()}}
to override for a specific league.
}
\description{
Returns the character vector of scoring categories where a lower value
corresponds to better standings performance (lower-is-better categories).
Used as the Layer-3 fallback in \code{\link[=sgp_denominators]{sgp_denominators()}} when neither a
per-call override nor a \code{\link[=league_config]{league_config()}} declaration is present.
}
\details{
The built-in list covers pitcher ratio stats: ERA, WHIP, FIP, xFIP,
SIERA, xERA, BB/9, HR/9. Higher-is-better pitching categories (K/9, K\%,
K-BB\%) are NOT included. Declare a custom set via
\code{\link[=league_config]{league_config()}}'s \code{inverse_categories} argument and pass the config
to \code{\link[=sgp_denominators]{sgp_denominators()}}.
}
\examples{
inverse_categories()

}
\seealso{
\code{\link[=league_config]{league_config()}}, \code{\link[=sgp_denominators]{sgp_denominators()}}
}
